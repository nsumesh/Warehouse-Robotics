slam_toolbox:
  ros__parameters:
    use_sim_time: true

    map_frame: map
    odom_frame: odom
    base_frame: base_footprint
    scan_topic: /scan

    mode: mapping
    resolution: 0.05
    publish_period: 1.0
